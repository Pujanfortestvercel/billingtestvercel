# Android Release Keystore Configuration Example
# Copy this file to keystore.properties in the same directory and fill in your actual credentials.
# NEVER commit keystore.properties containing actual passwords into Git version control.

storePassword=your_keystore_password_here
keyPassword=your_key_password_here
keyAlias=your_key_alias_here
storeFile=release.keystore
